@model  Post
@{
    ViewData["Title"] = "Post";
}

<h2>@Model.Title</h2>

@if (!String.IsNullOrEmpty(Model.Image))
{
    var image_path = $"/Image/{Model.Image}";
    <img src=@image_path/>
}

<p>@Model.Body</p>
